# 変更の記録

版ごとの変更点。Release の本文はここから写す。

## 0.9.0 — 2026-09-14

エンジンが推論に何を使っているか（CUDA / DirectML / CPU）を見て、NVIDIA の GPU なら
CUDA 版への切り替えを案内するようにした版（2026-09-14 の決定、Issue #2）。

- **推論の方式を出す。** 準備（`isready`）のときに `info string … provider cuda|dml|cpu` を出すエンジン
  （天秤将棋の拡張エンジン Libra）について、「USI ログ」に「推論: DirectML」のように出し、「エンジン」の一覧にも添える
- **NVIDIA の GPU で DirectML か CPU のまま読んでいると、CUDA 版への切り替えを案内する。** Libra の配布物は DirectML 版で、
  DLL を CUDA 版に差し替えると RTX 5070 Ti の実測で約 4 倍速い。CUDA 12 と cuDNN 9（計 2GB 前後）は NVIDIA から入れる
  必要があり、このアプリにもエンジンにも入っていない。案内は「今後表示しない」で止められ、「エンジン」の一覧から開き直せる
- **切り替えようとして DLL が足りなかったときは、その理由を出す。** Libra は落ちずに CPU などで読み続けるので、
  エラー文（足りない DLL の名前が入っている）と CUDA・cuDNN の置き場所を案内する
- 対局が進んでいる間と棋譜解析の途中は窓で盤を塞がず、状態の行に 1 行出して、止まってから案内を開く
- GPU は DXGI で調べる（窓は出ない）

## 0.8.1 — 2026-09-13

不具合を直した版。

- **駒台の持ち駒を選んだとき、選択の色が縦に大きくはみ出していたのを直した。** 駒台に玉の枠が無いとき（本将棋に入ってから・
  対局が終わったあと・局面編集）は歩の枠が 2 列ぶんの幅になるが、高さも 2 倍になっていて、選んだときやマウスを乗せたときの
  色が歩の上下に広がっていた。枠の高さを他の駒と揃えた
- **対局の席でも、外の布石エンジンにルールの版（`Fuseki_Rules`）を送るようにした。** 検討と棋譜解析では
  `Fuseki_Rules` を名乗るエンジンに送っていたが、対局だけ送っておらず、二飛香を加える前の棋譜から外のエンジンに
  続きを指させると、エンジン側の盤が反則手で止まっていた。0.8.0 の「外の布石エンジンにはルールの版を送らない」は
  この版から「名乗ったエンジンには送る」に変わる（名乗らないエンジンはこれまでどおり）

## 0.8.0 — 2026-09-13

天秤将棋に禁じ手「二飛香（にひきょう）」を加えた版（2026-09-13 のルール設計者の決定、Issue #1）。
**布石将棋は変えていない。**

- **天秤将棋の布石（3〜40 手目）で、二飛香を禁じ手にした。** 飛と香は自陣の同じ筋に合わせて 1 枚までしか
  打てず、すでに自分の飛か香がある筋に飛・香を打つ手は反則。数えるのは自分の駒だけ。40 手目の制限と一緒に掛かり、
  遮る手が二飛香で打てなければその手は無い（遮る手が 1 つも無ければ 41 手目の裁定。二飛香は外れない）
- 判定は同梱の wasm（cppshogi）に委ねている。公開サイト fuseki-shogi-web `e8bd37f` で作り直した
  `fuseki.{mjs,wasm}` に差し替えた（対応するソースは fork `kotenbu135/DeepLearningShogi` の `5cf6c9c`）。
  古い wasm を読んだときは、起動時に旗の値を照合して落ちる（黙って二飛香の効かない盤にしない）
- **二飛香の前に指された天秤将棋の棋譜も開ける。** 棋譜にルールの版は書かないので、二飛香に当たって入らず
  旧ルールなら入る天秤将棋の棋譜は、旧ルールで並べ直して「二飛香を加える前のルールで指された棋譜なので、
  そのルールで再生しています」と知らせる（対局の表題も「天秤将棋（二飛香を加える前のルール）」）。
  待った・分岐・過去の局面・検討・棋譜解析も同じ版で並べる。公開サイトと同じ扱い
- どちらのルールでも入らない棋譜は、何手目のどの手で止まったかを言って断るようにした（これまでは手数が出なかった）。
  読めなかったとき、今の対局の盤が読みかけの棋譜に上書きされたまま残る不具合も直した
- 内蔵の布石評価（方策・価値ネット）は、対局と同じルールで候補を並べる。重みは旧ルールで学習したものをそのまま使う
  （入力は盤面と持ち駒だけ）。外の布石エンジンにはルールの版を送らないので、二飛香を知らないエンジンが
  反則手を返したときは、これまでどおり 1 度聞き直してから止まる
- 局面編集は本将棋の局面を組む口（布石を置く口ではない）なので、二飛香は掛けない

## 0.7.0 — 2026-09-13

棋譜の形を [tenbinshogi.com](https://tenbinshogi.com/) と 1 つに揃えた版。書き出した KIF は
そのまま将棋所や ShogiHome で開け、このアプリで開き直せば玉の配置から並べ直せる。

- **tenbinshogi.com の「棋譜をコピー」を貼り付けて、玉の配置から再生できるようにした。**
  サイトの棋譜は 41 手目の局面を局面図に置いた普通の KIF（＝将棋所や ShogiHome でも開ける）で、
  布石 40 手と先後の選択はヘッダタグ `天秤布石：K*5i …` / `天秤選択：sente` に USI で入っている。
  このタグがあれば天秤モードとして読み、1 手目の玉から並べ直す。従来の方言
  （`手合割：天秤将棋` ＋ `５九玉打`）はこれまでどおり読める
- **書き出しもサイトと同じ 1 つの形にした（天秤将棋）。** 「棋譜を保存」は 41 手目の局面図から
  始まる普通の KIF を書き、布石 40 手と先後の選択はヘッダタグに入れる。**将棋所や ShogiHome で
  そのまま開けて、このアプリで開き直せば玉の配置から並べ直せる**ので、保存のときに
  「対局全体」と「本将棋の部分だけ」を選ぶのをやめた（布石将棋は方言のままなので、
  あちらでは今までどおり選べる）。布石の消費時間はサイトの棋譜には無いので、
  `天秤布石消費時間：3/3 - 1/4 …` のタグで残す（数が合わなければ時間だけ捨てて棋譜は読む）
- **KIF として読めない文字を貼ったとき、黙って平手の初期配置を出すのをやめた。**
  `parseKifHeader` は局面図が無くても平手を返すので、棋譜でない文字列が「0 手の平手」として
  読めてしまっていた（サイトの旧「手順をコピー」＝USI の一行がこれに当たり、貼ると平手が出た）。
  棋譜の見出しも局面図も指し手も無ければ読めないと言って断る

## 0.6.0 — 2026-09-11

布石の模型を最終世代 iter2455 に差し替え、世代を差し替える口を入れた版。

- **布石の模型（世代）を差し替えられるようにした。** エンジンの登録画面に「布石の模型（世代）」の欄を足し、
  書き出した世代のフォルダ（`models.json` と 3 つの重み）を足せる。同梱は消せず、足したぶんだけ外せる。
  **同梱の模型は動かしていない**（配布物の差し替えは学習を止めてから 1 回だけ行う）
- **席ごと・検討の欄ごとに世代を選べるようにした。** 対局の席に別々の世代を割り当てられるので、
  世代どうしを指させて目で比べられる（強さの判定は開発機のアリーナで測る。数局で分かる差ではない）
- 模型のフォルダは Rust 側の `read_binary_file`（生バイトを返す）で読む。`fetch` を使わないので
  CSP（`connect-src`）は開けていない
- 両玉の価値表が方策と別の世代だったとき、これまでは黙って閉じていた理由を出すようにした
  （一覧に「両玉の表なし」の印と、食い違った世代の名前）
- **同梱の布石模型を iter1177 → iter2455 に差し替えた。** 学習は 2026-09-11 に打ち切り、
  最終世代は iter2455。直接対局 1,600 局で **70.3% ± 1.1%（+149.5 Elo）**
  （昇格ゲートの鎖ではなく直接対局の値）
- **両玉の価値表を実対局で測り直した**（合計 72,750 局）。帯 42 組・平均 V=50.0%・
  偏りの中央値 2.3pt・最悪 5.7pt。選別に使った対局を値に一度も使っていない
  （旧 iter1177 の表は同じ標本で選んで測っていたので、最悪 6.1pt は過小評価だった）

## 0.5.0 — 2026-09-10

GPU で読むエンジン（dlshogi・ふかうら王）に対応した版。

- **GPU で読むエンジンを扱えるようにした。** 申告に `DNN_` の項目があればそう見なし、
  準備（isready）を既定 20 分まで待つ（模型を GPU 向けに組み直す初回が長い）。
  待っている間はエンジンが出す行を画面に出す。待つ秒数はエンジンごとに変えられる
- **GPU で読むエンジンも 1 押しで入る。**「はじめに」に「GPU で読むエンジンを入れる」を足した。
  dlshogi with GCT（WCSC31 版・約 67MB）を公式の配布先から取り、実行ファイル・DLL・モデルを
  まとめて置いて登録する。**CUDA も TensorRT も要らない**（同梱の DirectML で動くので、
  DirectX 12 の GPU なら NVIDIA でなくてもよい）。既定にするのは、本将棋の既定がまだ無いときだけ
  （水匠5 を使っている人の設定を変えない）。新しい dlshogi のモデルは配布元がパスワードを掛けており
  転記も禁じているので、自動では入れない（手で入れる道は README に）
- **GPU を使うエンジンは同時に 1 本だけ立てる。** 席の数だけ模型を載せると VRAM が尽きて落ちるため。
  検討で空いているものは自動で畳んで譲り、対局中や読んでいる最中は断る。
  エンジン同士の対局では先後で同じ 1 本を使い回す
- **エンジンの起動を待つ間、時計を止める。** 41 手目で模型を読み始めると、
  1 手も指さないうちに時間切れになっていた
- **勝率の目盛りを申告から読む。** dlshogi 系が申告する `Eval_Coef` は cp = 係数 · ln(p/(1−p)) の係数で、
  こちらの式の逆なので、そのまま S に使える（当てはめが要らない）。申告しない古い版のための
  既定も 600 から **756**（配布元のソースの係数）に直した
- **落ちたときに終了コードを出す。** GPU のエンジンは CUDA や TensorRT の DLL が足りないと
  標準エラーに 1 行も出さずに即死するので、終了コードが唯一の手がかりになる。
  終了は標準出力の EOF ではなく process の死で見るようにした（EOF では終了コードを拾えない）
- **取得中に何 MB 取れたかを出す。** 67MB を黙って待たせない（水匠5 の取り込みにも効く）
- エンジンの一覧と設定画面が GPU のエンジンの項目に合うようにした
  （`UCT_Threads`・`DNN_Model`・`DNN_Batch_Size` を上に出す、模型のファイルを参照ボタンで選べる）
- エンジンを入れ直すとき、動いているエンジンを先に止めるようにした
  （Windows は走っている実行ファイルと読み込み中の DLL を上書きさせない）

**0.4.1 以前から更新する方へ**: 既に登録してある dlshogi・ふかうら王は、
「エンジンの設定 → 申告を読む」を一度押すと GPU として扱われます（押すまでは以前の扱いのままです）。

開発のためのもの（使う分には関係しない）。

- 書庫（zip）の取り出しにテストを足し、CI の Windows の仕事を `cargo check` から
  `cargo test` に変えた（取り出しが実際に走るようにするため）

## 0.4.1 — 2026-09-10

直したもの。

- **エンジンが落ちたときに時計が止まらなかった。** 指す者が居ないまま自分の持ち時間だけが
  減り、そのまま時間切れになっていた。落ちたら対局を止めて知らせる
- **エンジンが二度続けて指せない手を返したときの警告が、出た直後に消えていた。**
  一時停止の描き直しが状態の行を上書きしていた
- **誰も拾わなかった失敗が無言だった。** 対局中に何かが失敗しても画面は黙って止まるだけだったのを、
  状態の行に赤で出すようにした
- **「エンジンを自動で入れる」が配布元の変化に弱かった。** 取ってきた書庫の SHA-256 を照合し、
  つながらない・止まったときに待ち続けず、失敗したら手で入れる道筋を案内する。
  書庫は流しながら書く（丸ごとメモリに載せない）

開発のためのもの（使う分には関係しない）。

- **テストが 1 本も走っていなくても緑になっていた。** `npm test` はシェルの glob 頼みで、
  `**` が効かないうえ、一致 0 件でも成功で終わっていた。`scripts/test.mjs` が数えて 0 なら落とす
- **CI を足した**（`.github/workflows/ci.yml`）。push と PR で型・テスト・ビルド・clippy と、
  Windows でアプリが通るかを見る
- KIF の読みと持ち時間の書式にテストを足した（12 → 22 本）
- 版を上げる作業を `npm run bump 0.4.2` の 1 回にまとめた（5 箇所の手作業をやめる）

## 0.4.0 — 2026-09-10

- 画面を英語でも出せるようにした（ツールバー右の English / 日本語。符号も西洋式になる。
  KIF は言葉に関わらず日本語のまま）
- アプリの名前を「天秤将棋」から「天秤将棋GUI」に変えた。**Windows は名前で別のアプリと見なすため、
  0.4.0 は 0.3.0 を上書きせず隣に入る**（README の片づけの案内を参照）
- エンジンの手番では盤を触らせない。止めている間は時計も止める
- 先後が決まったら候補手の欄を空にする
- ボタンに焦点があるときの Space が二重に効いていたのを直した

## 0.3.0 — 2026-09-09

- エンジンを 1 押しで入れられるようにした（やねうら王＋水匠5 を公式の配布先から取り、
  PC に合う実行ファイルを選んで登録する）
- 「はじめに」を 1 画面に収め、既定を明るい地にした
- 検討中は盤を触れるようにし、対局の既定と案内を利用者目線に直した

## 0.2.0 — 2026-09-09

最初の配布。Windows のインストーラ（NSIS）と自動更新。
