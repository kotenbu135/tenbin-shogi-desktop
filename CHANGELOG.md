# 変更の記録

版ごとの変更点。Release の本文はここから写す。

## 未リリース

- **布石の模型（世代）を差し替えられるようにした。** エンジンの登録画面に「布石の模型（世代）」の欄を足し、
  書き出した世代のフォルダ（`models.json` と 3 つの重み）を足せる。同梱は消せず、足したぶんだけ外せる。
  **同梱の模型は動かしていない**（配布物の差し替えは学習を止めてから 1 回だけ行う）
- **席ごと・検討の欄ごとに世代を選べるようにした。** 対局の席に別々の世代を割り当てられるので、
  世代どうしを指させて目で比べられる（強さの判定は開発機のアリーナで測る。数局で分かる差ではない）
- 模型のフォルダは Rust 側の `read_binary_file`（生バイトを返す）で読む。`fetch` を使わないので
  CSP（`connect-src`）は開けていない
- 両玉の価値表が方策と別の世代だったとき、これまでは黙って閉じていた理由を出すようにした
  （一覧に「両玉の表なし」の印と、食い違った世代の名前）

## 0.5.0 — 2026-09-10

GPU で読むエンジン（dlshogi・ふかうら王）に対応した版。

- **GPU で読むエンジンを扱えるようにした。** 申告に `DNN_` の項目があればそう見なし、
  準備（isready）を既定 20 分まで待つ（模型を GPU 向けに組み直す初回が長い）。
  待っている間はエンジンが出す行を画面に出す。待つ秒数はエンジンごとに変えられる
- **GPU で読むエンジンも 1 押しで入る。**「はじめに」に「GPU で読むエンジンを入れる」を足した。
  dlshogi with GCT（WCSC31 版・約 67MB）を公式の配布先から取り、実行ファイル・DLL・モデルを
  まとめて置いて登録する。**CUDA も TensorRT も要らない**（同梱の DirectML で動くので、
  DirectX 12 の GPU なら NVIDIA でなくてもよい）。既定にするのは、本将棋の既定がまだ無いときだけ
  （水匠5 を使っている人の設定を変えない）。新しい dlshogi のモデルは配布元がパスワードを掛けており
  転記も禁じているので、自動では入れない（手で入れる道は README に）
- **GPU を使うエンジンは同時に 1 本だけ立てる。** 席の数だけ模型を載せると VRAM が尽きて落ちるため。
  検討で空いているものは自動で畳んで譲り、対局中や読んでいる最中は断る。
  エンジン同士の対局では先後で同じ 1 本を使い回す
- **エンジンの起動を待つ間、時計を止める。** 41 手目で模型を読み始めると、
  1 手も指さないうちに時間切れになっていた
- **勝率の目盛りを申告から読む。** dlshogi 系が申告する `Eval_Coef` は cp = 係数 · ln(p/(1−p)) の係数で、
  こちらの式の逆なので、そのまま S に使える（当てはめが要らない）。申告しない古い版のための
  既定も 600 から **756**（配布元のソースの係数）に直した
- **落ちたときに終了コードを出す。** GPU のエンジンは CUDA や TensorRT の DLL が足りないと
  標準エラーに 1 行も出さずに即死するので、終了コードが唯一の手がかりになる。
  終了は標準出力の EOF ではなく process の死で見るようにした（EOF では終了コードを拾えない）
- **取得中に何 MB 取れたかを出す。** 67MB を黙って待たせない（水匠5 の取り込みにも効く）
- エンジンの一覧と設定画面が GPU のエンジンの項目に合うようにした
  （`UCT_Threads`・`DNN_Model`・`DNN_Batch_Size` を上に出す、模型のファイルを参照ボタンで選べる）
- エンジンを入れ直すとき、動いているエンジンを先に止めるようにした
  （Windows は走っている実行ファイルと読み込み中の DLL を上書きさせない）

**0.4.1 以前から更新する方へ**: 既に登録してある dlshogi・ふかうら王は、
「エンジンの設定 → 申告を読む」を一度押すと GPU として扱われます（押すまでは以前の扱いのままです）。

開発のためのもの（使う分には関係しない）。

- 書庫（zip）の取り出しにテストを足し、CI の Windows の仕事を `cargo check` から
  `cargo test` に変えた（取り出しが実際に走るようにするため）

## 0.4.1 — 2026-09-10

直したもの。

- **エンジンが落ちたときに時計が止まらなかった。** 指す者が居ないまま自分の持ち時間だけが
  減り、そのまま時間切れになっていた。落ちたら対局を止めて知らせる
- **エンジンが二度続けて指せない手を返したときの警告が、出た直後に消えていた。**
  一時停止の描き直しが状態の行を上書きしていた
- **誰も拾わなかった失敗が無言だった。** 対局中に何かが失敗しても画面は黙って止まるだけだったのを、
  状態の行に赤で出すようにした
- **「エンジンを自動で入れる」が配布元の変化に弱かった。** 取ってきた書庫の SHA-256 を照合し、
  つながらない・止まったときに待ち続けず、失敗したら手で入れる道筋を案内する。
  書庫は流しながら書く（丸ごとメモリに載せない）

開発のためのもの（使う分には関係しない）。

- **テストが 1 本も走っていなくても緑になっていた。** `npm test` はシェルの glob 頼みで、
  `**` が効かないうえ、一致 0 件でも成功で終わっていた。`scripts/test.mjs` が数えて 0 なら落とす
- **CI を足した**（`.github/workflows/ci.yml`）。push と PR で型・テスト・ビルド・clippy と、
  Windows でアプリが通るかを見る
- KIF の読みと持ち時間の書式にテストを足した（12 → 22 本）
- 版を上げる作業を `npm run bump 0.4.2` の 1 回にまとめた（5 箇所の手作業をやめる）

## 0.4.0 — 2026-09-10

- 画面を英語でも出せるようにした（ツールバー右の English / 日本語。符号も西洋式になる。
  KIF は言葉に関わらず日本語のまま）
- アプリの名前を「天秤将棋」から「天秤将棋GUI」に変えた。**Windows は名前で別のアプリと見なすため、
  0.4.0 は 0.3.0 を上書きせず隣に入る**（README の片づけの案内を参照）
- エンジンの手番では盤を触らせない。止めている間は時計も止める
- 先後が決まったら候補手の欄を空にする
- ボタンに焦点があるときの Space が二重に効いていたのを直した

## 0.3.0 — 2026-09-09

- エンジンを 1 押しで入れられるようにした（やねうら王＋水匠5 を公式の配布先から取り、
  PC に合う実行ファイルを選んで登録する）
- 「はじめに」を 1 画面に収め、既定を明るい地にした
- 検討中は盤を触れるようにし、対局の既定と案内を利用者目線に直した

## 0.2.0 — 2026-09-09

最初の配布。Windows のインストーラ（NSIS）と自動更新。
