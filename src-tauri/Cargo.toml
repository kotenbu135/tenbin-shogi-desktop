[package]
name = "tenbin-shogi-gui"
version = "0.5.0"
description = "天秤将棋のデスクトップGUI（対局・検討）"
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
name = "tenbin_shogi_gui_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
tauri-plugin-store = "2"
tauri-plugin-opener = "2"
tauri-plugin-process = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
usi-host = { path = "../crates/usi-host" }
encoding_rs = "0.8"
# おすすめのエンジンを取り込むため（配布物は 7z）
reqwest = { version = "0.13", default-features = false, features = ["rustls", "webpki-roots", "http2", "charset"] }
sevenz-rust2 = "0.22"
# GPU で読むエンジン（dlshogi）の配布物は zip。中身は deflate だけ
zip = { version = "4", default-features = false, features = ["deflate"] }
# 取ってきた書庫の中身の照合（配布元の貼り替えに気づくため）
sha2 = "0.10"

# 自動更新はデスクトップだけ（モバイルには入れない）
[target.'cfg(any(target_os = "macos", windows, target_os = "linux"))'.dependencies]
tauri-plugin-updater = "2"
