[package]
name = "usi-host"
version = "0.1.0"
description = "USIエンジンの子プロセスを起動し、標準入出力を行単位で仲介する（Tauri非依存）"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
# エンジンの出力は日本語 Windows だと Shift_JIS のことがある
encoding_rs = "0.8"

[dev-dependencies]
